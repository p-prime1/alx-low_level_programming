#include "main.h"
/**
 * _islower - Checks if int is in lower case
 * @c: Upper or Lower case
 * Return: 1 or 0
 */

int _islower(int c)
{
	if (c != 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
