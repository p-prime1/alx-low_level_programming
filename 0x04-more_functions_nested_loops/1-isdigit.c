#include "main.h"

/**
 * _isdigit - Checks for digit
 * @c: Digit to be checked
 * Return: 1 or 0
 */

int _isdigit(int c)
{
	if (_isdigit(int c) == 0)
	{
		return 0;
	}
	else
	{
		return (1);
	}
}


